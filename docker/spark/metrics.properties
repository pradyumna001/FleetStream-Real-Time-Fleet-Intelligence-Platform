# Expose Spark metrics in Prometheus format at <ui>/metrics/prometheus.
# Spark 3.5 ships PrometheusServlet natively, so no external JMX exporter or
# sidecar is needed for the driver-side streaming metrics we care about
# (batch duration, input rate, processing rate, state store size).
*.sink.prometheusServlet.class=org.apache.spark.metrics.sink.PrometheusServlet
*.sink.prometheusServlet.path=/metrics/prometheus
master.sink.prometheusServlet.path=/metrics/master/prometheus
applications.sink.prometheusServlet.path=/metrics/applications/prometheus

# Source instrumentation
*.source.jvm.class=org.apache.spark.metrics.source.JvmSource
